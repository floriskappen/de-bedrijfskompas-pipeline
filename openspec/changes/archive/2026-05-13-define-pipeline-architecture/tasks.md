## Tasks

This change is architectural and has no implementation tasks. The spec at `specs/pipeline-architecture/spec.md` is the deliverable. Archiving this change promotes it into `openspec/specs/` as binding constraints for subsequent stage changes.

- [x] Capture stage sequence, seam contract, and file layout as a spec
