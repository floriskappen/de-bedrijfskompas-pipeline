## Tasks

This change is architectural and has no implementation tasks. The MODIFIED requirements in `specs/pipeline-architecture/spec.md` are the deliverable. Archiving promotes them into `openspec/specs/pipeline-architecture/spec.md` as binding constraints for the upcoming `implement-content-collection` change.

- [x] Capture the merged `content-collection` stage and relaxed seam/layout rules as MODIFIED requirements
