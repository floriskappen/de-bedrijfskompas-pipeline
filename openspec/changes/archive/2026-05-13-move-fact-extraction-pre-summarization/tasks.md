## Tasks

This change is architectural and has no implementation tasks. The MODIFIED Stage Sequence requirement in `specs/pipeline-architecture/spec.md` is the deliverable. Archiving rewrites `openspec/specs/pipeline-architecture/spec.md` with the linear order `content-collection → fact-extraction → content-summarization → theme-analytic stages → dataset-output`.

- [x] Capture the linear stage order with `fact-extraction` upstream of `content-summarization` as a MODIFIED Stage Sequence requirement
