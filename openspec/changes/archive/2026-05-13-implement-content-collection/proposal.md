# Proposal: Implement the content collection stage


---

## Notes (original brainstorm)

Okay, so for this stage, in which we do the content collection, we have the websites from the previous stage, so that's the input, the names of the companies, of course, and the websites, and have been resolved for the ones that weren't there. And well, now we can already assume that the websites are correct, because that needs to have, should be checked in the previous one. There can be like companies which have no website because they got an error in the previous stage, so this then is too bad. We must skip them. We should keep track of it somewhere, though, like we should keep propagating it to the next stages, because in the end, of course, we will want a summary of kind of which companies failed and which companies succeeded. Currently, this is just on disk data, so we will save markdown files of every page, basically. On disk, but later we will use the database. We will save it in there. But yeah, that's something for later. So the idea is to basically crawl the website in kind of a smart way. I don't know if there is like some best practice to do this or something, but basically what we need to do is we need to It would be best to, like, kind of get the... So we need to grab... We need to grab not just the homepage, but take the top-end links with kind of, so we need the target known paths for sure, which should be Dutch and English for now. We can take the end links of the homepage, like where it links to, to get like maybe some projects or whatever, but we need some depth limit. We definitely need to have three to eight high-quality pages. So we definitely need durable content. We do not, we want to skip for sure like things like blog or news, although sometimes a blog can contain good information or news, but I think it's safer to just skip it for now. And yeah. Failure looks like. I'm not getting enough pages, or... That's it, I think, we're not like being able to, like, getting errors or like being blocked or something from the crawling. Because I think we won't actually judge the content of these pages too much, because that will be in the content summarization, I think, in that stage. And we'll have to, of course, later on, we need to be able to say, like, at the quality of what we scraped, to see, like, if we got, like, missed a lot or something, I don't know. Yeah. Maybe it's also good to kind of try to get the sitemap or try to... I don't know. I don't know, no, because that's like changed a lot per site, of course. But I mean, if there's a sitemap or something like that, I don't know. I think it's good to just then also get something from that, just to know, like, how much percent of the website we scraped, because there's... But I don't know if that's useful. Maybe for later, actually. Let's just keep it simple for now and just, yeah, try and do that. And then we need to basically parse the HTML. A simple way to do it is kind of... I think what I did earlier was just using indeed trafilatura. So yeah, just use that to... Not include images, but just include tables. I think, yeah, and some other settings may be useful. The duplicate would be nice to enable. I don't know what favorite position exactly does. And some other settings, maybe you can, since you know what the goal this is, you can like think about what the best settings are for trafilatura. And just use that, basically. Yeah.